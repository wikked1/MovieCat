{
  "name": "descbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rebuild": "npx shx rm -rf dist/* && tsc",
    "build": "npm run rebuild",
    "launch": "node ./dist/bot.js",
    "start": "npm run rebuild && npm run launch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.3.1"
  },
  "devDependencies": {
    "shx": "^0.3.3",
    "typescript": "^4.2.4",
    "typesync": "^0.8.0"
  }
}
